<!--
  Example launch file for octomap_server mapping:
  Listens to incoming PointCloud2 data and incrementally builds an octomap.
  The data is sent out in different representations.

  Copy this file into your workspace and adjust as needed, see
  www.ros.org/wiki/octomap_server for details
-->
<launch>
	<node pkg="octomap_server" exec="octomap_server_node" name="octomap_server">
		<param name="resolution" value="0.05" />

		<!-- fixed map frame (set to 'map' if SLAM or localization running!) -->
		<param name="frame_id" value="livox_mid360" />
		<param name="base_frame_id" value="base_link" />


		<!-- maximum range to integrate (speedup!) -->
		<param name="sensor_model.max_range" value="5.0" />

		<param name="occupancy_min_z" value="0.1" />
		<param name="occupancy_max_z" value="1.0" />

		<param name="filter_ground_plane" value="true" />

		<param name="use_height_map" value="true" />

		<!-- data source to integrate (PointCloud2) -->
		<remap from="cloud_in" to="livox_mid360_PointCloud2" />

		<!-- <param name="octomap_path" value="/home/rm/sim2real/test4_ws/map.bt" /> -->
	</node>
</launch>
